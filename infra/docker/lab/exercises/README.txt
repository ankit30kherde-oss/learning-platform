Welcome to the lab.

This container has no network access and you are not root. Everything you
create here is destroyed when the session ends, so experiment freely.

Suggested warm-up:
  ls -la
  cat /etc/os-release
  ps aux
  id
  df -h
