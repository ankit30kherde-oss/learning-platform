# Default (isolated) node linker: every app gets its own node_modules, which is
# what lets each service generate its OWN Prisma client without clobbering the
# others. Do not switch to node-linker=hoisted.
auto-install-peers=true
strict-peer-dependencies=false
enable-pre-post-scripts=true
